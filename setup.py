from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A project on predicting number of pickups within regions for uber drivers',
    author='Shivam',
    license='',
)
